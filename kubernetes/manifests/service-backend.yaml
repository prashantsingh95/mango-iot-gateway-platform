apiVersion: v1
kind: Service
metadata:
  name: iot-backend
  namespace: iot-platform
  labels:
    app: iot-backend
    tier: backend
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: http
  selector:
    app: iot-backend
